# Strava OAuth credentials
# Create an app at https://www.strava.com/settings/api
# Set Authorization Callback Domain to your app's host (e.g. localhost for local dev)
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# Base URL of this app (optional but recommended)
# Must match Strava's Authorization Callback Domain. Examples:
#   http://localhost:5000
#   http://127.0.0.1:5000
#   https://yourdomain.com
# If unset, Flask derives it from the request, which can fail (e.g. localhost vs 127.0.0.1 mismatch).
BASE_URL=

# Flask session encryption
SECRET_KEY=
